services:
  web:
    build:
      context: ../..
      dockerfile: apps/web/Dockerfile.nginx
    image: family-hub-web:prod
    ports:
      - "${WEB_PORT:-8080}:80"
    depends_on:
      api:
        condition: service_healthy
