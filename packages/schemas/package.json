{
  "name": "@family-hub/schemas",
  "version": "0.1.0",
  "private": true,
  "description": "family-hub shared Zod schemas (single source of truth)",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "pnpm -w tsc -p tsconfig.json",
    "typecheck": "pnpm -w tsc -p tsconfig.json --noEmit",
    "lint": "pnpm -w eslint \"src/**/*.ts\"",
    "format": "pnpm -w prettier --write \"src/**/*.ts\"",
    "test": "jest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
